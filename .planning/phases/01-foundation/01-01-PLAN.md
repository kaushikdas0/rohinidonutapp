---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/theme/theme.ts
  - src/vite-env.d.ts
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04

must_haves:
  truths:
    - "Running npm run dev starts the app in a browser with no errors"
    - "A MUI Button renders with the custom ThemeProvider applied"
    - "A Phosphor Icon renders visibly in the app"
    - "All text renders in Quicksand font, not browser default"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies"
      contains: "@mui/material"
    - path: "src/main.tsx"
      provides: "Entry point with ThemeProvider and CssBaseline"
      contains: "ThemeProvider"
    - path: "src/theme/theme.ts"
      provides: "MUI theme stub with Quicksand font"
      contains: "Quicksand Variable"
    - path: "src/App.tsx"
      provides: "Verification component proving all requirements"
      contains: "phosphor-icons"
    - path: "vite.config.ts"
      provides: "Vite configuration for React"
      contains: "react"
  key_links:
    - from: "src/main.tsx"
      to: "src/theme/theme.ts"
      via: "import { theme } from './theme/theme'"
      pattern: "import.*theme.*from.*theme"
    - from: "src/main.tsx"
      to: "@fontsource-variable/quicksand"
      via: "import '@fontsource-variable/quicksand'"
      pattern: "fontsource.*quicksand"
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "ThemeProvider wrapping App"
      pattern: "ThemeProvider.*App"
---

<objective>
Scaffold the React + Vite project with MUI, Phosphor Icons, and Quicksand font fully wired up.

Purpose: Establish the tooling foundation so Phase 2 (Design System) can build on a working app with theme, icons, and typography ready.
Output: A running Vite dev server rendering a verification page that proves all four SETUP requirements are met.
</objective>

<execution_context>
@/Volumes/x10/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/x10/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project and wire all dependencies</name>
  <files>
    package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json,
    vite.config.ts, index.html, src/main.tsx, src/App.tsx,
    src/theme/theme.ts, src/vite-env.d.ts
  </files>
  <action>
1. Scaffold the project using Vite's react-ts template. Since we are already in the project directory (rohinidonutapp), scaffold into a temp directory and move files up, or use `npm create vite@latest . -- --template react-ts` if it supports in-place scaffolding. Preserve existing .git and .planning directories.

2. Install all dependencies:
   ```
   npm install @mui/material @emotion/react @emotion/styled
   npm install @phosphor-icons/react
   npm install @fontsource-variable/quicksand
   ```

3. Create `src/theme/theme.ts` with a minimal MUI theme stub:
   - Set `typography.fontFamily` to `'"Quicksand Variable", sans-serif'` (note the nested quotes -- this is critical per research pitfall #1)
   - Leave palette and component overrides empty for Phase 2 to fill in
   - Export the theme object

4. Update `src/main.tsx` to:
   - Import `@fontsource-variable/quicksand` (this loads the variable font with weights 300-700)
   - Import `ThemeProvider` and `CssBaseline` from `@mui/material`
   - Import the theme from `./theme/theme`
   - Wrap `<App />` in `<ThemeProvider theme={theme}>` with `<CssBaseline />` inside
   - Keep `<React.StrictMode>` as outer wrapper

5. Replace `src/App.tsx` with a verification component that proves all four requirements:
   - Import and render a `<Typography variant="h3">` with "Rohini Donut App" (proves SETUP-04: Quicksand font)
   - Import and render a `<Typography variant="body1">` with explanatory text (proves font on body too)
   - Import and render a Phosphor icon (check phosphoricons.com for exact donut icon name; fall back to `Cookie`, `CircleNotch`, or similar round icon if "Donut" doesn't exist). Use `size={64} weight="duotone"` (proves SETUP-03)
   - Import and render a `<Button variant="contained">` (proves SETUP-02: MUI + ThemeProvider)
   - Wrap everything in `<Container maxWidth="sm">` with centered text and margin-top

6. Remove any Vite boilerplate files not needed: `src/App.css`, `src/index.css` (if CssBaseline replaces it), default Vite SVG assets. Keep `src/vite-env.d.ts`.

7. Verify build succeeds: run `npm run build` and confirm no errors.
  </action>
  <verify>
    <automated>cd /Volumes/x10/CODE/rohinidonutapp && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run build` completes with zero errors
    - package.json contains @mui/material, @phosphor-icons/react, @fontsource-variable/quicksand
    - src/main.tsx wraps App in ThemeProvider with CssBaseline
    - src/theme/theme.ts exports a theme with Quicksand Variable font family
    - src/App.tsx renders MUI components and a Phosphor icon
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify foundation visually</name>
  <files>none</files>
  <action>
    Start the dev server with `npm run dev` and present the URL to the user for visual verification.

    What was built: Complete React + Vite project with MUI, Phosphor Icons, and Quicksand font wired up.

    How to verify:
    1. Run `npm run dev` in the project root (should start on http://localhost:5173)
    2. Open the URL in a browser
    3. Verify these four things:
       - Page loads without console errors (SETUP-01)
       - "MUI Button Works" button renders with MUI styling, not plain HTML (SETUP-02)
       - A Phosphor icon (donut or similar) renders visibly at 64px (SETUP-03)
       - ALL text (heading + body) renders in Quicksand font -- rounded, modern letterforms, NOT browser default serif/sans-serif (SETUP-04)

    Resume signal: Type "approved" or describe any issues.
  </action>
  <verify>
    <automated>cd /Volumes/x10/CODE/rohinidonutapp && npm run build</automated>
  </verify>
  <done>User confirms all four SETUP requirements are visually verified in the browser.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run dev` starts dev server on localhost:5173
- Browser shows MUI Button with theme styling
- Browser shows Phosphor Icon rendered
- Browser shows text in Quicksand font (rounded letterforms)
- No console errors in browser dev tools
</verification>

<success_criteria>
All four SETUP requirements are met and visually confirmed:
1. SETUP-01: Dev server runs, app loads
2. SETUP-02: MUI components render with ThemeProvider
3. SETUP-03: Phosphor icon visible in the app
4. SETUP-04: All text in Quicksand font
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
